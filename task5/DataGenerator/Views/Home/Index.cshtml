<html>
<body>
    <header>
        <nav class="navbar fixed-top navbar-expand-lg navbar-light bg-white border-bottom box-shadow mb-2">
            <div class="container-fluid">
                <div class="d-flex align-items-center">
                    <span>|| saikat8486@gmail.com ||</span>
                </div>

                <div class="d-flex align-items-center">
                    <label class="fw-bold me-2">Region</label>
                    <select id="regionId" asp-items="ViewBag.regionList" class="form-select"></select>
                </div>

                <div class="d-flex align-items-center">
                    <label class="fw-bold me-2">Error</label>
                    <input id="errorSlide" type="range" min="0" max="10" value="0">&nbsp;
                    <input id="errorInput" type="number" min="0" max="1000" value="0" />
                </div>

                <div class="d-flex align-items-center">
                    <label class="fw-bold me-2">Seed</label>
                    <input id="seedInput" type="number" min="1" value="" /><button class="btn btn-outline-secondary" id="btnRandomSeed"><i class="bi bi-shuffle"></i></button>
                </div>

                <div class="d-flex align-items-center">
                    <button class="btn btn-outline-secondary" id="btnExport"><b>CSV</b> <i class="bi bi-file-earmark-arrow-up"></i></button> &nbsp;
                </div>
            </div>
        </nav>
    </header>

    <div class="row pt-5">
        <table id="tblData" class="table table-bordered table-striped">
            <thead>
                <tr>
                    <th>Serial</th>
                    <th>User Id</th>
                    <th>Name</th>
                    <th>Address</th>
                    <th>Phone</th>
                </tr>
            </thead>

        </table>
    </div>
</body>
</html>
<script src="~/js/loadUsers.js"></script>